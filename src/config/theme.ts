import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  colors: {
    primary: "#ff014f",
    secondary: "#85F4FF",
    tertiary: "#B8FFF9",
  },
});

export default theme;
